apiVersion: v1
kind: Service
metadata:
  name: glooscap-operator-api
  namespace: system
  labels:
    control-plane: controller-manager
spec:
  selector:
    control-plane: controller-manager
  ports:
    - name: http-api
      port: 3000
      targetPort: http-api

