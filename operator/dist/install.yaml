apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
    control-plane: controller-manager
  name: glooscap-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: translationjobs.wiki.glooscap.dasmlab.org
spec:
  group: wiki.glooscap.dasmlab.org
  names:
    kind: TranslationJob
    listKind: TranslationJobList
    plural: translationjobs
    singular: translationjob
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TranslationJob is the Schema for the translationjobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TranslationJob
            properties:
              destination:
                description: Destination indicates where translated content should
                  be published.
                properties:
                  languageTag:
                    description: LanguageTag sets the desired language annotation.
                    type: string
                  pathPrefix:
                    description: PathPrefix ensures translated pages use a specific
                      prefix (e.g., language code).
                    type: string
                  targetRef:
                    description: TargetRef overrides the target wiki; defaults to
                      source target.
                    type: string
                type: object
              parameters:
                additionalProperties:
                  type: string
                description: Parameters includes optional overrides for translation
                  prompts or throttling.
                type: object
              pipeline:
                default: TektonJob
                description: Pipeline selects the execution mode for the translation.
                enum:
                - InlineLLM
                - TektonJob
                type: string
              source:
                description: Source identifies the wiki target and page to translate.
                properties:
                  pageId:
                    description: PageID is the identifier of the Outline page to translate.
                    type: string
                  revision:
                    description: Revision allows locking translation to a specific
                      revision.
                    type: string
                  targetRef:
                    description: TargetRef refers to the WikiTarget resource name.
                    type: string
                required:
                - pageId
                - targetRef
                type: object
            required:
            - source
            type: object
          status:
            description: status defines the observed state of TranslationJob
            properties:
              auditRef:
                description: AuditRef references an immutable audit log entry.
                type: string
              conditions:
                description: Conditions provide granular status updates.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              duplicateInfo:
                description: DuplicateInfo contains information about a duplicate
                  page found at destination.
                properties:
                  message:
                    description: Message is a human-readable message about the duplicate.
                    type: string
                  pageId:
                    description: PageID is the ID of the duplicate page at the destination.
                    type: string
                  pageTitle:
                    description: PageTitle is the title of the duplicate page.
                    type: string
                  pageUri:
                    description: PageURI is the URI of the duplicate page.
                    type: string
                required:
                - message
                - pageId
                - pageTitle
                - pageUri
                type: object
              finishedAt:
                description: FinishedAt records when processing completed.
                format: date-time
                type: string
              message:
                description: Message contains human-readable details about the current
                  state.
                type: string
              startedAt:
                description: StartedAt records when processing began.
                format: date-time
                type: string
              state:
                description: State reflects the high-level lifecycle phase.
                enum:
                - Queued
                - Validating
                - AwaitingApproval
                - Dispatching
                - Running
                - Publishing
                - Completed
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: translationservices.wiki.glooscap.dasmlab.org
spec:
  group: wiki.glooscap.dasmlab.org
  names:
    kind: TranslationService
    listKind: TranslationServiceList
    plural: translationservices
    singular: translationservice
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Translation service address
      jsonPath: .spec.address
      name: Address
      type: string
    - description: Service type
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Connection status
      jsonPath: .status.connected
      name: Connected
      type: boolean
    - description: Registration status
      jsonPath: .status.registered
      name: Registered
      type: boolean
    - description: Overall status
      jsonPath: .status.status
      name: Status
      type: string
    - description: Client ID
      jsonPath: .status.clientId
      name: ClientID
      type: string
    - description: Ready condition
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TranslationService is the Schema for the translationservices
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of TranslationService
            properties:
              address:
                description: Address is the gRPC address of the translation service
                  (e.g., iskoces-service.iskoces.svc.cluster.local:50051)
                maxLength: 512
                type: string
              secure:
                default: false
                description: Secure enables TLS/mTLS for the connection
                type: boolean
              type:
                description: Type specifies the translation service type (e.g., "iskoces",
                  "nanabush")
                enum:
                - iskoces
                - nanabush
                type: string
            required:
            - address
            - type
            type: object
          status:
            description: Status defines the observed state of TranslationService
            properties:
              clientId:
                description: ClientID is the client identifier assigned by the translation
                  service after registration
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the service's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connected:
                default: false
                description: Connected indicates whether the gRPC connection is established
                type: boolean
              heartbeatIntervalSeconds:
                description: HeartbeatIntervalSeconds is the interval between heartbeats
                  in seconds
                type: integer
              lastHeartbeat:
                description: LastHeartbeat records the timestamp of the last heartbeat
                  received
                format: date-time
                type: string
              missedHeartbeats:
                description: MissedHeartbeats counts how many heartbeats have been
                  missed
                type: integer
              registered:
                default: false
                description: Registered indicates whether the client has successfully
                  registered with the service
                type: boolean
              status:
                description: Status is the overall connection status (e.g., "healthy",
                  "warning", "error")
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: wikitargets.wiki.glooscap.dasmlab.org
spec:
  group: wiki.glooscap.dasmlab.org
  names:
    kind: WikiTarget
    listKind: WikiTargetList
    plural: wikitargets
    singular: wikitarget
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WikiTarget is the Schema for the wikitargets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WikiTarget
            properties:
              insecureSkipTLSVerify:
                default: true
                description: |-
                  InsecureSkipTLSVerify when true, skips TLS certificate verification for HTTPS connections.
                  This is useful for self-signed certificates or internal wikis without proper CA certificates.
                type: boolean
              isPaused:
                default: false
                description: IsPaused when true, stops reconciliation of this WikiTarget.
                type: boolean
              mode:
                description: Mode determines how this target will be used during publication.
                enum:
                - ReadOnly
                - ReadWrite
                - PushOnly
                type: string
              serviceAccountSecretRef:
                description: ServiceAccountSecretRef references the Kubernetes secret
                  containing API credentials.
                properties:
                  key:
                    description: Key within the secret data map. Defaults to "token".
                    type: string
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
              sync:
                description: Sync configures the cadence of page discovery.
                properties:
                  fullRefreshInterval:
                    description: FullRefreshInterval ensures a complete rescan at
                      the provided cadence.
                    type: string
                  interval:
                    description: Interval represents how often discovery should run.
                    type: string
                type: object
              translationDefaults:
                description: TranslationDefaults specifies default destination parameters
                  when creating TranslationJobs.
                properties:
                  destinationPathPrefix:
                    description: DestinationPathPrefix adds a prefix (e.g., language
                      code) for translated pages.
                    type: string
                  destinationTarget:
                    description: DestinationTarget optionally overrides the target
                      wiki for translated content.
                    type: string
                  languageTag:
                    description: LanguageTag sets the BCP 47 language code for translations.
                    type: string
                type: object
              uri:
                description: URI is the base URL of the Outline wiki to synchronise.
                format: uri
                maxLength: 512
                type: string
            required:
            - mode
            - serviceAccountSecretRef
            - uri
            type: object
          status:
            description: status defines the observed state of WikiTarget
            properties:
              catalogRevision:
                description: CatalogRevision increments each time the catalogue is
                  refreshed.
                format: int64
                type: integer
              collectionID:
                description: |-
                  CollectionID stores the ID of the target collection (e.g., "Maurice (PGD") once found.
                  This avoids re-searching for the collection on every refresh.
                type: string
              collectionName:
                description: CollectionName stores the name of the target collection
                  for reference.
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of a target's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime records the most recent successful discovery
                  run.
                format: date-time
                type: string
              paused:
                default: false
                description: Paused indicates whether reconciliation is currently
                  paused for this WikiTarget.
                type: boolean
              ready:
                default: false
                description: Ready indicates whether the WikiTarget has completed
                  at least one successful discovery.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-controller-manager
  namespace: glooscap-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-leader-election-role
  namespace: glooscap-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: operator-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - batch
  resources:
  - jobs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs
  - translationservices
  - wikitargets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs/finalizers
  - translationservices/finalizers
  - wikitargets/finalizers
  verbs:
  - update
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs/status
  - translationservices/status
  - wikitargets/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: operator-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: operator-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-translationjob-admin-role
rules:
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs
  verbs:
  - '*'
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-translationjob-editor-role
rules:
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-translationjob-viewer-role
rules:
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - translationjobs/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-wikitarget-admin-role
rules:
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - wikitargets
  verbs:
  - '*'
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - wikitargets/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-wikitarget-editor-role
rules:
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - wikitargets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - wikitargets/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-wikitarget-viewer-role
rules:
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - wikitargets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - wiki.glooscap.dasmlab.org
  resources:
  - wikitargets/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-leader-election-rolebinding
  namespace: glooscap-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: operator-controller-manager
  namespace: glooscap-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
  name: operator-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: operator-manager-role
subjects:
- kind: ServiceAccount
  name: operator-controller-manager
  namespace: glooscap-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: operator-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: operator-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: operator-controller-manager
  namespace: glooscap-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
    control-plane: controller-manager
  name: operator-controller-manager-metrics-service
  namespace: glooscap-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: operator
    control-plane: controller-manager
---
apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: operator-glooscap-operator-api
  namespace: glooscap-system
spec:
  ports:
  - name: http-api
    port: 3000
    targetPort: http-api
  selector:
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: operator
    control-plane: controller-manager
  name: operator-controller-manager
  namespace: glooscap-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: operator
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: operator
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        env:
        - name: GLOOSCAP_API_ADDR
          value: :3000
        - name: VLLM_JOB_NAMESPACE
          value: nanabush
        - name: VLLM_JOB_IMAGE
          value: ghcr.io/dasmlab/glooscap-translation-runner:latest
        - name: VLLM_API_URL
          value: http://vllm.nanabush.svc:8000
        - name: NANABUSH_GRPC_ADDR
          value: 209.15.95.244:50051
        - name: NANABUSH_SECURE
          value: "false"
        image: ghcr.io/dasmlab/glooscap:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 3000
          name: http-api
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      imagePullSecrets:
      - name: dasmlab-ghcr-pull
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: operator-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []
